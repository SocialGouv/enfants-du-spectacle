
import React from "react";
import styles from "./Card.module.scss";

const Card: React.FC = ({}) => {

    return (
        <div className={styles.cardContainer}>
            
        </div>
    );
};

export default Card;