app:
  strategyType: Recreate
  addVolumes:
    - docs
  securityContext:
    fsGroup: 1000

form:
  strategyType: Recreate
  addVolumes:
    - docs-form
  securityContext:
    fsGroup: 1000

pg:
  backup:
    name: "202403061907" # backup on a new folder

pg-form:
  backup:
    name: "202403061907" # backup on a new folder
